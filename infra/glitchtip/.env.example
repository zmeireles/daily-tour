# Copy to .env on the box (chmod 600). Generate secrets, never commit real values:
#   SECRET_KEY=$(openssl rand -hex 32)
#   GT_PG_PASS=$(openssl rand -hex 24)
SECRET_KEY=change_me_openssl_rand_hex_32
GT_PG_PASS=change_me_openssl_rand_hex_24
